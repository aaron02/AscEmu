# Forever protocol profile

Forever is a modern Battle.net client and therefore selects the same Battle.net server topology as Midnight.

The protocol itself is intentionally a separate profile. No Midnight opcode, packet, authentication-key or build constant is copied into this directory as a placeholder. Add those files only when an exact Forever client build is available and verified.
